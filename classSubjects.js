// Declare variables and assign values
let subjectTitle = "Data Structure's and algorithm";
let classSchedule = "Monday, 8:00 AM - 9:00 AM";
let classroom = "Comlab B";
let classInstructor = "Professor Pascua";
let studentName = "Franz Bugasto";
// Log the information in the specified format
console.log(`${studentName} is currently enrolled in ${subjectTitle} with a class schedule of ${classSchedule} at room ${classroom}. The instructor for the subject is ${classInstructor}.`);
